export default function Loading() {

    return (
        <body className={`bg-slate-900 w-screen h-screen flex justify-center items-center flex-col`}>
            <h1 className='text-white text-3xl mb-6'>Loading...</h1>
            <h2 className='text-slate-400 text-lg'>Wait a bit until we prepare everything for you!</h2>
        </body>
    )
}